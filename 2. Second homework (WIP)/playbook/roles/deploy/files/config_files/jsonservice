server {
    listen 80;
    listen 443 ssl;
    server_name 192.168.0.29;
    ssl_certificate     /etc/ssl/certs/jsonservice.crt;
    ssl_certificate_key /etc/ssl/private/jsonservice.key;
    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/jsonservice.sock;
    }
}